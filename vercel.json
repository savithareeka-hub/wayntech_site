{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/apps/web/dist/$1" }
  ],
  "buildCommand": "cd apps/web && npm run build",
  "outputDirectory": "apps/web/dist"
}